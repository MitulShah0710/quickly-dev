#if(!$util.isNullOrBlank($context.arguments.tenant))
    #set( $tenant_HASH = $context.arguments.tenant)
    #set( $requester = "API" )
#else
    #set( $tenant_HASH = $ctx.identity.claims.get("custom:TENANT_HASH"))
    #set( $requester = $ctx.identity.username )
#end
{
    "version": "2017-02-28",
    "operation": "Invoke",
    "payload": {
        "field": "getLocation",
        "arguments":  $utils.toJson($context.arguments),
        "tenantHash": $utils.toJson($tenant_HASH)
    }
}